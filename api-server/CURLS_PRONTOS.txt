════════════════════════════════════════════════════════════════
  📡 CURLS PRONTOS PARA COPIAR E COLAR
════════════════════════════════════════════════════════════════

🔥 ATENÇÃO: Primeiro inicie a API com "npm start" nesta pasta!

════════════════════════════════════════════════════════════════


✅ CURL 1 - ENVIAR MENSAGEM DE TEXTO
────────────────────────────────────────────────────────────────

curl -X POST http://localhost:3001/api/send-message -H 'Content-Type: application/json' -d '{"message": "Olá! Temos uma promoção especial hoje! 🎉", "sender": "Jyze Bot"}'


────────────────────────────────────────────────────────────────


✅ CURL 2 - ENVIAR IMAGEM (COM URL DA IMAGEM)
────────────────────────────────────────────────────────────────

curl -X POST http://localhost:3001/api/send-image -H 'Content-Type: application/json' -d '{"imageUrl": "https://picsum.photos/400/300", "caption": "Confira nossa promoção! 🔥", "sender": "Marketing"}'


────────────────────────────────────────────────────────────────


💡 TROCAR A URL DA IMAGEM:
────────────────────────────────────────────────────────────────

Substitua "https://picsum.photos/400/300" pela URL da sua imagem:

curl -X POST http://localhost:3001/api/send-image -H 'Content-Type: application/json' -d '{"imageUrl": "SUA_URL_AQUI", "caption": "Seu texto aqui", "sender": "Nome"}'


────────────────────────────────────────────────────────────────


📝 MAIS EXEMPLOS:
────────────────────────────────────────────────────────────────

# Mensagem simples
curl -X POST http://localhost:3001/api/send-message -H 'Content-Type: application/json' -d '{"message": "Estamos online!"}'

# Imagem de produto
curl -X POST http://localhost:3001/api/send-image -H 'Content-Type: application/json' -d '{"imageUrl": "https://exemplo.com/produto.jpg", "caption": "Novo produto disponível!"}'

# Promoção
curl -X POST http://localhost:3001/api/send-message -H 'Content-Type: application/json' -d '{"message": "🔥 PROMOÇÃO: 20% OFF até meia-noite!"}'


════════════════════════════════════════════════════════════════
  ✅ PRONTO! As mensagens aparecerão no chat automaticamente!
════════════════════════════════════════════════════════════════

